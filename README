My dotfiles compilation

Branches are used for different configurations

MASTER is a jack of all trades for linux desktop

GITBASH is a very simple config set used on git bash on Windows

On linux, GNU stow is used to link all to the $HOME dir

On GITBASH there is a script to link the files on ~/

Plug is used for vim extensions.

zsh grml is used as a base zsh config

