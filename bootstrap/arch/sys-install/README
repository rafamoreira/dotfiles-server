ARCH INSTALL SCRIPTS
====================

This is the scripts that I use for arch full install.

ATTENTION
=========

All scripts are very simple, but tailored to my system needs specifically, but not only, the partitioning step.
Edit it first to avoid data loss.
